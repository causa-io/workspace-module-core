version: 1

workspace:
  name: causa

project:
  name: workspace-module-core
  description: The Causa workspace module providing core function definitions and some implementations.
  language: typescript
  type: package

causa:
  modules:
    '@causa/workspace-core': '>= 0.26.1'
    '@causa/workspace-typescript': '>= 0.16.1'

javascript:
  dependencies:
    check:
      allowlist: []
    update:
      packageTargets:
        '@types/node': minor

model:
  schema: jsonschema

  codeGenerators:
    - generator: typescriptModelClass
      globs: [./src/configurations/schemas/*.yaml]
      nestedSchemas: []
      output: src/configurations/generated.ts
